import { CreatedDateDirective } from './created-date.directive';

describe('CreatedDateDirective', () => {
  it('should create an instance', () => {
    const directive = new CreatedDateDirective();
    expect(directive).toBeTruthy();
  });
});
